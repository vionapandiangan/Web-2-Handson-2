// Numeric enum
enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
}

// String enum
enum Color {
    Red = "RED",
    Green = "GREEN",
    Blue = "BLUE",
}

console.log(Direction.Down);
console.log("Color: ", Color.Red);